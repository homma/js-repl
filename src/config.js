/*
 * @author Daisuke Homma
 */

const jsrepl = {};
jsrepl.config = {};

{ // namespace boundary

const config = jsrepl.config;

config.rootElementId = "js-repl";
config.prompt = ">";
config.resultPrompt = "=>";

config.debug = false;

} // namespace boundary
